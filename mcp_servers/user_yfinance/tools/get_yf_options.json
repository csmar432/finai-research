{
  "name": "get_yf_options",
  "description": "获取期权链数据（到期日、行使价、希腊值）",
  "parameters": {
    "type": "object",
    "properties": {
      "ticker": { "type": "string", "description": "股票代码，例如 'AAPL'" }
    },
    "required": ["ticker"]
  }
}
